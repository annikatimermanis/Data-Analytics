Data Analytics Concordia University

Titanic Dataset
􏰀 Perform data analytics
􏰀 Develop a story/storytelling

Tip Rate
􏰀 pandas
􏰀 Data types: nominal, ordinal, interval, or ratio

Data Wrangling
􏰀 construct a text facet when given a column with text strings as entries;
􏰀 construct a numeric cluster when given a column with numeric entries, with the option of using a log scale;
􏰀 cluster a column of text entries (using an appropriate clustering method);
􏰀 trimming all whitespace at the start and end of a string; 
􏰀 replacing internal whitespace by a single space.
